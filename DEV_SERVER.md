# 프론트 개발 서버 (`localhost:3000`)

## `ERR_CONNECTION_REFUSED` 가 나올 때

브라우저가 **연결을 거부**했다는 것은, 그 순간 **PC 안에 `3000` 포트에서 대기 중인 Vite(개발 서버)가 없다**는 뜻입니다. “코드가 깨져서”가 아니라, **서버를 안 켰거나, 켠 터미널을 닫았거나(Ctrl+C), 다른 오류로 프로세스가 바로 종료**된 경우가 대부분입니다.

### 올바른 실행 방법 (둘 중 하나)

1. **저장소 루트**에서 (한 번에 프론트만):

   ```bash
   npm run dev
   ```

   내부적으로 `frontend` 폴더에서 `vite`가 실행됩니다.

2. **`frontend` 폴더로 들어간 뒤**:

   ```bash
   cd frontend
   npm run dev
   ```

터미널에 `Local: http://localhost:3000/` 이 보이면, **그 터미널은 닫지 말고** 브라우저에서 같은 주소로 접속하세요.

### 하지 말 것

- **`frontend\apps`** 는 Python 예제용 폴더입니다. 여기서 `npx vite`만 하면 설정·포트가 기대와 다를 수 있습니다. 프론트는 **`frontend`** 또는 **루트의 `npm run dev`** 만 쓰세요.
- 백엔드만 켜 두고 프론트 터미널을 끈 뒤 `localhost:3000` 만 열면 항상 거부됩니다. (API는 `8000`, UI는 `3000` — **둘 다** 필요합니다.)

### 포트 3000 이 이미 사용 중일 때

`vite.config.ts` 에 `strictPort: true` 가 있어, 3000이 다른 프로그램에 잡혀 있으면 `npm run dev` 가 **시작 단계에서 실패**합니다. 다른 터미널의 옛날 `npm run dev` 를 종료하거나, 작업 관리자에서 해당 Node 프로세스를 끄고 다시 시도하세요.

### 백엔드(8000)와 같이 쓸 때

백엔드: `cd backend\apps` 후 `python main.py`  
프론트: 위 `npm run dev` — **서로 다른 터미널**에서 동시에 실행한 채로 두세요.

## 폰·태블릿·웹뷰 앱에서 채팅·API가 안 될 때

PC 브라우저에서 `http://127.0.0.1:3000` 으로 열면, 같은 페이지의 `fetch("/chat")` 는 **Vite 개발 서버가 대신** 백엔드(`127.0.0.1:8000`)로 넘겨 줍니다.

**모바일이나 다른 기기**에서 `localhost` / `127.0.0.1` 을 쓰면, 그 주소는 **그 기기 안**을 가리키므로 Vite·백엔드에 닿지 않습니다.

### 같은 Wi‑Fi 에서 모바일 브라우저로 테스트

1. PC에서 `npm run dev` 실행 후, 터미널에 나오는 **`Network` 주소**(예: `http://192.168.0.10:3000`)를 폰 브라우저에 입력합니다. (Windows 에서 처음 연결 시 방화벽이 Node.js 허용을 물으면 **허용**하세요.)
2. 백엔드는 그대로 PC에서 `python main.py` 로 두면 됩니다. 폰이 `…:3000/chat` 으로 요청하면 **프록시는 PC 의 Vite**가 처리하므로, API가 `127.0.0.1` 만 듣고 있어도 됩니다.

### 빌드된 앱·웹뷰가 `http://…:8000` 을 직접 호출하는 경우

정적 파일이나 WebView 에서 `VITE_API_BASE` 로 백엔드 전체 URL을 쓰는 경우, 폰이 **PC 의 LAN IP** 로 접속해야 합니다.

1. `frontend/.env` 에 예: `VITE_API_BASE=http://192.168.0.10:8000` 후 `npm run build`.
2. 백엔드 실행 시 PC 에서만 들을 게 아니라면: `API_HOST=0.0.0.0` (Windows PowerShell 예: `$env:API_HOST="0.0.0.0"; python main.py`). **공용 Wi‑Fi에서는 지양**하고, 집/사내망에서만 쓰세요.
